{
  "name": "greenlight",
  "private": true,
  "engines": {
    "node": "15.0.1"
  },
  "scripts": {
    "build-client": "cd client && npm install --production=false && npm run build",
    "build-cordova": "npm run build-client && rsync -a --delete --remove-source-files client/build/ cordova/www",
    "run-cordova-ios": "npm run build-cordova && cd cordova && cordova run ios",
    "run-cordova-android": "npm run build-cordova && cd cordova && cordova run android",
    "run-cordova-browser": "npm run build-cordova && cd cordova && cordova run browser",
    "heroku-postbuild-future": "REACT_APP_HEROKU_APP_NAME=$HEROKU_APP_NAME npm run build-client",
    "cordova-init": "cd cordova && cordova platform add ios && cordova platform add android",
    "run-client": "cd client && npm start",
    "run-server": "rails s",
    "run-service-worker": "bundle exec sidekiq",
    "start": "concurrently \"npm run run-client\" \"npm run run-server\" \"npm run run-service-worker\"",
    "test:e2e": "cypress run --headed --browser chrome",
    "test:e2e-chromium": "cypres run --headless --browser chromium",
    "test:e2e:watch": "cypress open",
    "lint": "eslint --ext .ts --ext .tsx",
    "lint-dry": "eslint --ext .ts --ext .tsx --fix-dry-run src",
    "lint-fix": "eslint --ext .ts --ext .tsx --fix src"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^4.6.0",
    "concurrently": "^5.3.0",
    "cypress": "^5.5.0",
    "typescript": "^4.0.5"
  }
}
